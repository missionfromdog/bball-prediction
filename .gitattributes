# Git LFS tracking for DATA files only
# Models are small enough to commit directly

# Large data files (if needed in repo)
data/original/games.csv filter=lfs diff=lfs merge=lfs -text
data/original/games_details.csv filter=lfs diff=lfs merge=lfs -text
data/games_with_real_vegas.csv filter=lfs diff=lfs merge=lfs -text

# NOTE: Models are NOT tracked by LFS anymore
# They're small (<10MB each) and need to be accessible in workflows
